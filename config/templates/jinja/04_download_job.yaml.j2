{% if not (storage.hostPath is defined and storage.hostPath.enabled | default(false)) %}
apiVersion: batch/v1
kind: Job
metadata:
  name: {{ downloadJob.name }}
  namespace: {{ namespace.name }}
spec:
  backoffLimit: {{ downloadJob.backoffLimit }}
  template:
    metadata:
      namespace: {{ namespace.name }}
      labels:
        app: {{ labels.app }}
    spec:
      containers:
        - name: downloader
          image: {{ images.benchmark.repository }}:{{ images.benchmark.tag }}
          command: ["/bin/sh", "-c"]
          args:
            - |
              mkdir -p "${MOUNT_PATH}/${MODEL_PATH}" && \
              pip install -U --user huggingface_hub && \
              export PATH="${PATH}:${HOME}/.local/bin" && \
{% if huggingface.enabled %}
              hf auth login --token "${HF_TOKEN}" && \
{% endif %}
              hf download "${HF_MODEL_ID}" --local-dir "{{ downloadJob.mountPath }}/${MODEL_PATH}"
          env:
            - name: MODEL_PATH
              value: {{ model.path }}
            - name: HF_MODEL_ID
              value: {{ model.huggingfaceId }}
{% if huggingface.enabled %}
            - name: HF_TOKEN
              valueFrom:
                secretKeyRef:
                  name: {{ huggingface.secretName }}
                  key: {{ huggingface.tokenKey }}
{% endif %}
            - name: HF_HOME
              value: /tmp/huggingface
            - name: HOME
              value: /tmp
            - name: MOUNT_PATH
              value: {{ downloadJob.mountPath }}
          volumeMounts:
            - name: model-cache
              mountPath: {{ downloadJob.mountPath }}
      restartPolicy: OnFailure
{% if vllmCommon.pullSecret is defined and vllmCommon.pullSecret %}
      imagePullSecrets:
        - name: {{ vllmCommon.pullSecret }}
{% endif %}
      volumes:
        - name: model-cache
          persistentVolumeClaim:
            claimName: {{ storage.modelPvc.name }}
{% endif %}
