# This is the no-FMA baseline scenario for WVA. Topology:
# Qwen3-32B served by 1 modelservice decode pod, scaled by WVA's HPA.
# Apples-to-apples with the WVA+FMA arm (cicd/ocp-wva-fma-warmstart): both disable
# vLLM prefix caching so the comparison measures the scaling path, not the
# prefix cache.
#
# Running:
#
#   llmdbenchmark --spec cicd/ocp-wva standup -p <namespace>
scenario:
  - name: "ocp-wva-cicd"

    kustomize:
      enabled: false

    # =========================================================================
    # MODEL CONFIGURATION
    # Qwen3-32B: large enough that the per-request KV cache footprint is
    # significant on H100 (~16GB free for KV after weights), so the heavy
    # profile drives gpu_cache_usage_perc into the saturating range WVA's
    # optimizer reads. Same model as guides/workload-autoscaling.yaml .
    # =========================================================================
    model:
      name: Qwen/Qwen3-32B
      shortName: qwen-qwen3-32b
      path: models/Qwen/Qwen3-32B
      huggingfaceId: Qwen/Qwen3-32B
      size: 1Ti
      maxModelLen: 16000
      blockSize: 64
      gpuMemoryUtilization: 0.95

    # =========================================================================
    # DEPLOYMENT METHOD
    # =========================================================================
    modelservice:
      enabled: true

    standalone:
      enabled: false

    gateway:
      className: epponly

    # =========================================================================
    # WVA CONFIGURATION
    #
    # Edit any field below to change WVA behavior for this scenario.
    # Remove a field to fall back to its default.
    # =========================================================================
    wva:
      enabled: true

      # Upstream well-lit-path tag.
      wellLitPath: optimized-baseline

      # Where to install the WVA controller.
      # Empty = use the deploy namespace (the first slot of `-p`). Override by
      # passing `-p deploy,harness,wva` to put the controller elsewhere.
      # NOTE: With namespaceScoped=true (below), the controller ONLY watches
      # this namespace.
      namespace: ""

      # Ref of llm-d/llm-d used for the WVA kustomize overlay (CRD + RBAC +
      # controller manifests). Consumed by 19_wva-kustomize.yaml.j2.
      repoRef: "main"

      # Controller container image.
      image:
        repository: quay.io/braulio/llm-d-wva
        tag: keda3

      replicaCount: 1                # WVA controller pod replicas (1 is the chart default)

      # Disable to skip installing the controller but still render the HPA
      controller:
        enabled: true

      # Namespaced mode: controller only watches its own namespace. One WVA
      # controller per unique wva.namespace across all rendered stacks.
      # Set to false ONLY if you intend a single controller to manage
      # multiple namespaces (cluster-scoped mode — discouraged on shared
      # clusters because controllers from different tenants will race on
      # the same VAs).
      namespaceScoped: true

      # Controller-side metrics endpoint (scraped by user-workload-monitoring).
      # Don't change unless you have a port collision.
      metrics:
        enabled: true
        port: 8443                   # /metrics port on the controller
        secure: true                 # HTTPS + bearer-token auth

      # Where the controller queries to read vLLM metrics for its scaling
      # decisions. On OpenShift this is thanos-querier; on GKE point it at
      # your kube-prometheus-stack Service URL.
      prometheus:
        baseUrl: https://thanos-querier.openshift-monitoring.svc.cluster.local
        port: 9091

      # -----------------------------------------------------------------------
      # VariantAutoscaling — per-model scaling intent
      #
      # Only `variantCost` is consumed: it becomes the `llm-d.ai/variant-cost`
      # annotation on the KEDA ScaledObject. The WVA controller reads its SLO /
      # saturation tuning from the ConfigMaps shipped by the upstream overlay
      # (applied in step_03), NOT from per-model fields here — this scenario
      # renders no VariantAutoscaling CR. The replica floor/ceiling comes from
      # the hpa.min/maxReplicas block below.
      # -----------------------------------------------------------------------
      variantAutoscaling:
        # Relative GPU cost weight used by WVA's saturation solver to decide
        # WHICH model to scale when several share GPU capacity. Higher =
        # more expensive to scale up. Common tiering: H100=10, A100=8,
        # L40S=5, MI300X=10. Tweak per stack to express your business priority.
        variantCost: "10.0"

      # -----------------------------------------------------------------------
      # KEDA ScaledObject — what actually changes the replica count
      #
      # Named `hpa` for parity with the upstream guide, but these knobs drive a
      # KEDA ScaledObject (28_wva-scaledobject.yaml.j2), not a native HPA +
      # prometheus-adapter. KEDA queries Prometheus for `wva_desired_replicas`
      # (emitted by the WVA controller) and scales the decode Deployment between
      # min/max. KEDA generates an HPA under the hood, so `behavior` applies.
      # -----------------------------------------------------------------------
      hpa:
        enabled: true                # gates the smoketest's autoscaling-aware
                                     # replica-range check (does not affect
                                     # rendering; the ScaledObject is gated on
                                     # wva.enabled)
        minReplicas: 1               # ScaledObject minReplicaCount — never scale
                                     # below this; must be >= 1 (KEDA/HPA refuse
                                     # to manage targets at 0)
        maxReplicas: 6               # ScaledObject maxReplicaCount — safety
                                     # ceiling regardless of what the controller
                                     # computes
        targetAvgValue: 1            # Prometheus trigger threshold for
                                     # `wva_desired_replicas`. 1 = "match the
                                     # controller's desiredReplicas exactly".
                                     # Don't change unless you understand
                                     # HPA/KEDA averaging math.

        # Scaling behavior — how aggressively the HPA reacts. See:
        #   https://kubernetes.io/docs/tasks/run-application/horizontal-pod-autoscale/#configurable-scaling-behavior
        behavior:
          scaleUp:
            # 30s (down from upstream's 120) so HPA reacts quickly when WVA
            # trips saturation — important for benchmark runs where the
            # configured load exceeds 1-replica capacity. Default 120s
            # leaves ~2 min of queue growth before relief, which pushes
            # requests past the harness's 300s per-request timeout. 30s
            # is still long enough to avoid flap.
            stabilizationWindowSeconds: 30
            policies:
              - type: Percent        # alternative: "Pods" (absolute pod count)
                value: 100           # 100% = double the current replica count per period
                periodSeconds: 15    # how often this policy is evaluated
          scaleDown:
            # Larger window for scale-down avoids thrashing when load
            # briefly dips. Bump to 300+ for batch workloads where transient
            # idle is normal.
            stabilizationWindowSeconds: 120
            policies:
              - type: Percent
                value: 100           # = halve the replica count per period
                periodSeconds: 15

      # vLLM *emulator* service — chart-internal feature for testing WVA
      # without real vLLM pods. We deploy real vLLM via the modelservice
      # chart, so we always disable this. Leave as-is.
      vllmService:
        enabled: false

    # =========================================================================
    # CHART VERSIONS
    #
    # The WVA controller itself is installed from the upstream kustomize
    # overlay (see wva.image.tag / wva.repoRef above), not a helm chart --
    # this block only pins helm charts still installed by step_03.
    # =========================================================================
    chartVersions:
      # prometheus-adapter chart (prometheus-community/prometheus-adapter).
      # Pinned because newer releases have broken external-metric
      # compatibility with the rule format the WVA chart emits.
      # The upstream WVA README also pins this version.
      prometheusAdapter: 5.2.0

    # =========================================================================
    # ROUTING / GAIE CONFIGURATION
    # =========================================================================
    # WVA autoscaling is driven by EPP queue depth, so we must enable the
    # `flowControl` feature gate in the EPP's EndpointPickerConfig. That gate
    # is not on by default, and toggling it requires shipping a custom
    # EndpointPickerConfig (the default one the gaie chart ships has no
    # `featureGates` block). We inline it here and point `pluginsConfigFile`
    # at this filename so the chart mounts it as the active config.
    router:
      epp:
        pluginsConfigFile: "wva-plugins.yaml"
        pluginsCustomConfig:
          wva-plugins.yaml: |
            apiVersion: llm-d.ai/v1alpha1
            kind: EndpointPickerConfig
            featureGates:
              - flowControl
            plugins:
              - type: queue-scorer
              - type: kv-cache-utilization-scorer
              - type: prefix-cache-scorer
              - type: no-hit-lru-scorer
            schedulingProfiles:
              - name: default
                plugins:
                  - pluginRef: queue-scorer
                    weight: 2
                  - pluginRef: kv-cache-utilization-scorer
                    weight: 2
                  - pluginRef: prefix-cache-scorer
                    weight: 3
                  - pluginRef: no-hit-lru-scorer
                    weight: 2

    # =========================================================================
    # PREFILL / DECODE CONFIGURATION (same as inference-scheduling.yaml)
    # =========================================================================
    prefill:
      enabled: false
      replicas: 0

    decode:
      replicas: 1

      initContainers:
        - name: preprocess
          imageKey: benchmark
          imagePullPolicy: Always
          command: ["set_llmdbench_environment.py", "-e", "/shared-config/llmdbench_env.sh", "-i"]
          volumeMounts:
            - name: shared-config
              mountPath: /shared-config

      parallelism:
        tensor: 1
        data: 1
        dataLocal: 1
        workers: 1

      resources:
        limits:
          memory: 64Gi
          cpu: "16"
        requests:
          memory: 64Gi
          cpu: "16"

      extraEnvVars: []

      extraContainerConfig:
        ports:
          - containerPort: 5557  # NIXL side channel port
            protocol: TCP
          - containerPort: 8200  # Metrics port
            name: metrics
            protocol: TCP
        securityContext:
          capabilities:
            add:
              - "IPC_LOCK"
              - "SYS_RAWIO"
          runAsGroup: 0
          runAsUser: 0
        imagePullPolicy: Always

      additionalVolumeMounts: []
      additionalVolumes: []

      # -----------------------------------------------------------------------
      # vLLM args
      #
      # `--no-enable-prefix-caching` is set here so the decode pod's vLLM
      # configuration matches the FMA launcher's (which hard-codes
      # `--no-enable-prefix-caching` in config/templates/jinja/24_fma-
      # deployment.yaml.j2). Without this, baseline gets a massive unfair
      # advantage on shared-prefix workloads (heavy profile = 4000-token
      # shared prefix × 32 reuses per group → ~16× fewer prefill tokens
      # with caching on). Keeping both sides apples-to-apples means we're
      # benchmarking the scaling/scheduling path, not vLLM's prefix cache.
      # -----------------------------------------------------------------------
      vllm:
        additionalFlags:
          - "--no-enable-prefix-caching"

      # -----------------------------------------------------------------------
      # Probes
      #
      # HTTP probes aligned with the upstream WVA well-lit-path guide
      # (llm-d/guides/workload-autoscaling/ms-workload-autoscaling/values.yaml):
      #   - startupProbe + readinessProbe use /v1/models — only returns 200
      #     once vLLM has finished loading weights, so "Ready" truly means
      #     "serving inference requests".
      #   - livenessProbe uses /health — catches vLLM crash-loops (our
      #     previous TCP-socket probe stayed green because the routing
      #     sidecar stayed up while vLLM died).
      #
      # All probes target the routing sidecar port (8000 = servicePort),
      # which proxies to vLLM on 8200. Probing 8000 exercises the full
      # routing path end-to-end — the right signal for "benchmarking-ready".
      # -----------------------------------------------------------------------
      probes:
        startup:
          path: /v1/models
          failureThreshold: 60
          initialDelaySeconds: 15
          periodSeconds: 30
          timeoutSeconds: 5
        liveness:
          path: /health
          failureThreshold: 3
          periodSeconds: 10
          timeoutSeconds: 5
        readiness:
          path: /v1/models
          failureThreshold: 3
          periodSeconds: 5
          timeoutSeconds: 2

      # -----------------------------------------------------------------------
      # PodMonitor (Prometheus scraping)
      #
      # Required for WVA: the WVA controller consumes vLLM metrics via
      # prometheus-adapter; no PodMonitor → no metrics → HPA never sees a
      # value for wva_desired_replicas → autoscaling silently broken.
      # The `release: llmd` label is what user-workload-monitoring keys
      # on for discovery, matching the upstream WVA guide's example.
      # -----------------------------------------------------------------------
      monitoring:
        podmonitor:
          enabled: true
          portName: metrics
          path: /metrics
          interval: 30s
          labels:
            release: llmd

    # =========================================================================
    # VLLM COMMON CONFIGURATION
    # =========================================================================
    vllmCommon:
      kvTransfer:
        enabled: true
        connector: NixlConnector
        role: kv_both

      flags:
        # enforceEager: false (overrides the defaults.yaml default of true).
        # CUDA graphs improve decode throughput ~10-30% on H100 by eliminating
        # per-kernel launch overhead. Symmetric with FMA's launcher, which has
        # never run with --enforce-eager (its options string in
        # 24_fma-deployment.yaml.j2 doesn't include it). With this set to true,
        # baseline got an unfair perf handicap that masked real architectural
        # comparisons.
        enforceEager: false
        disableLogRequests: true
        disableUvicornAccessLog: true
        allowLongMaxModelLen: "1"
        serverDevMode: "1"

      volumes:
        - name: shared-config
          type: emptyDir
          emptyDir: {}
        - name: dshm
          type: emptyDir
          emptyDir:
            medium: Memory
            sizeLimit: 16Gi

      volumeMounts:
        - name: dshm
          mountPath: /dev/shm
        - name: shared-config
          mountPath: /shared-config

    # =========================================================================
    # STORAGE
    # =========================================================================
    storage:
      modelPvc:
        size: 1Ti

    # =========================================================================
    # WORKLOAD / HARNESS
    # =========================================================================
    # Enable metrics scraping (collect_metrics.sh in the harness):
    # captures requester replica counts + EPP KV-cache/queue-depth gauges into
    # results/metrics/..
    monitoring:
      metricsScrapeEnabled: true

    workDir: "/tmp/llmdbenchcicd-ocp-wva/"

    harness:
      name: inference-perf
      # Heavy variant — same load as guides/workload-autoscaling.yaml so the
      # FMA pass and this baseline are exercised under matched RPS.
      experimentProfile: shared_prefix_synthetic_heavy.yaml
      # The heavy profile drives high RPS against Qwen3-32B; the infra
      # reusable does not pass --wait-timeout on the CLI so the CLI falls
      # back to this field (see cli.py harness_wait_timeout resolution).
      waitTimeout: 5400
