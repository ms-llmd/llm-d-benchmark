# OpenShift Qwen3-8B + Qwen3-32B IPP -- STANDUP/TEARDOWN spec (both pools).
# See scenarios/cicd/ocp-qwen3-8b-32b.yaml for the deep-research-agent story.
# Use cicd/ocp-qwen3-8b-32b-summarizer for `run` (single summarizer harness).

# [REQUIRED]
{% set base_dir = base_dir | default('../') -%}
base_dir: {{ base_dir }}

# [REQUIRED]
values_file:
  path: {{ base_dir }}/config/templates/values/defaults.yaml

# [REQUIRED]
template_dir:
  path: {{ base_dir }}/config/templates/jinja

# [OPTIONAL]
scenario_file:
  path: {{ base_dir }}/config/scenarios/cicd/ocp-qwen3-8b-32b.yaml
